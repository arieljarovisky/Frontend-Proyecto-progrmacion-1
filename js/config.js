/* 
 * Archivo: config.js
 * Descripción: Define la URL base del servidor backend para que el frontend pueda realizar las peticiones a la API.
 * Modificando esta constante, se actualizan todas las llamadas a la API en el proyecto.
 */

const API_BASE_URL = "http://127.0.0.1:5000"; 
